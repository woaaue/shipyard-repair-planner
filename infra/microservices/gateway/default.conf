server {
    listen 80;
    server_name _;

    location /health {
        add_header Content-Type text/plain;
        return 200 "gateway-ok";
    }

    location / {
        add_header Content-Type application/json;
        return 501 '{"message":"Gateway routes are not configured yet"}';
    }
}
